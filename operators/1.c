#include <stdio.h>

int main()
{
    char ch;
    puts("_/\\_Prudence here!");
    puts("Enter the character");
    ch = getchar();
    if(ch == 'x')
    {
        puts("The character entered equals X");
    }
    puts("_/\\_");
}